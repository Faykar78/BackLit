# Keyboard backlight hotkeys (Fn key not detectable, using Ctrl as modifier)
# Ctrl + KP_Multiply (*) = Toggle backlight on/off
"kb_toggle"
    control + KP_Multiply

# Ctrl + KP_Add (+) = Brightness up (brighter)
"kb_bright_up"
    control + KP_Add

# Ctrl + KP_Subtract (-) = Brightness down (dimmer)
"kb_bright_down"
    control + KP_Subtract

# Ctrl + KP_Divide (/) = Cycle color
"kb_color_cycle"
    control + KP_Divide
